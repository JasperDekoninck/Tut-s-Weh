import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        padding: 20,
    },
    question: {
        fontSize: 18,
        fontWeight: 'bold',
        marginBottom: 20,
    },
})